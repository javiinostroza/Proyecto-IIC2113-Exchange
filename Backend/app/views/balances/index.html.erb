<p id="notice"><%= notice %></p>

<h1>Balances</h1>

<table>
  <thead>
    <tr>
      <th>Amount</th>
      <th>Wallet</th>
      <th>Market</th>
      <th>Cryptocurrency</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @balances.each do |balance| %>
      <tr>
        <td><%= balance.amount %></td>
        <td><%= balance.wallet_id %></td>
        <td><%= balance.market_id %></td>
        <td><%= balance.cryptocurrency_id %></td>
        <td><%= link_to 'Show', balance %></td>
        <td><%= link_to 'Edit', edit_balance_path(balance) %></td>
        <td><%= link_to 'Destroy', balance, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Balance', new_balance_path %>
